Place optional runtime binaries for Tauri bundle resources in this directory.

This folder is intentionally tracked so local `cargo tauri dev` and `cargo tauri build`
do not fail when bundle resources are resolved.
