# Rust Desktop Frontend

Pure Rust desktop frontend for AI Report Formatter.

## Run

```bash
cargo run --manifest-path apps/rust-desktop/Cargo.toml
```

Or from repo root:

```bash
npm run dev:rust
```

## Features

- Import local markdown file
- Preview parsed payload as JSON
- Export DOCX using Rust backend core (`rust-api` crate)
